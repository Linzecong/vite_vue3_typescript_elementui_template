// 网站用到的ts类或接口放这里，也可以分文件

interface IUser {
    id: number;
    username: string;
    password: string;
    email: string;
    phone: string;
    role: string;
    createTime?: string;
    updateTime?: string;
}
